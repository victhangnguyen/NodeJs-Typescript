console.log('Something');
//! node app.ts => Run => So is Node capable of executing TypeScript
//! NO => It'sure
//! It's done, because it just doesn't care about he file extension
var age;
age = 30;
console.log(age);
//! node app.ts => Run => get Error
//! using tsc to compile TypeScript to JavaScript to node can run
